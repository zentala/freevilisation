node_modules/
dist/
dist-types/
.plan/
.env
.claude/
.turbo
*.tsbuildinfo
coverage/
.wrangler/

# opencode implementer feedback - harvested by oc-harvest, never committed
FEEDBACK.md
TASK.md

# opencode run harness state - snapshots, logs, ledger. Never committed.
.agents/

# Dispatch runtime ledger, context packages, and isolated worktrees are local evidence.
.dispatch/
